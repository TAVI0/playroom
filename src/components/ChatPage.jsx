// El chat en sí vive en Clippy (renderizado global desde Navbar) -- esta ruta
// solo existe para que Clippy detecte pathname === "/chat" y pase a su modo
// foco (centrado, fondo gris, protagonista de la pantalla).
export default function ChatPage() {
	return null;
}
